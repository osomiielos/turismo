const slider=document.querySelector(".slidermx"),leftButton=document.querySelector(".arrow-left"),rightButton=document.querySelector(".arrow-right");let currentSlide=0;const totalSlides=slider.children.length;function updateSlider(){slider.style.transform=`translateX(-${400*currentSlide}px)`}leftButton.addEventListener("click",()=>{currentSlide=0===currentSlide?totalSlides-1:currentSlide-1,updateSlider()}),rightButton.addEventListener("click",()=>{currentSlide=currentSlide===totalSlides-1?0:currentSlide+1,updateSlider()});
